# 前端环境变量示例：复制为 .env 后按需修改
# cp .env.example .env

# API 基础路径（与后端 /api/v1 前缀对齐，配合 vite.config.ts 中 /api 代理使用）
VITE_API_BASE=/api/v1

# 是否回退 mock 数据：
# - false（默认，不设置时也按 false 处理）：调用真实后端
# - true：使用 src/mock 数据（离线开发/单测回退）
VITE_USE_MOCK=false
